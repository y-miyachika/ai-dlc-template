{
  "name": "api-generator",
  "version": "1.0.0",
  "description": "Hono RPC based REST API generator from services layer. Generates HTTP layer (routes, schemas, types) from existing business logic.",
  "author": "AI-DLC Template",
  "tags": ["api", "hono", "rpc", "rest", "typescript", "codegen"],
  "requires": {
    "services": "Business logic layer (services/*.service.ts) must exist"
  },
  "generates": [
    "routes/*.ts - HTTP endpoint definitions",
    "schemas/*.ts - Zod validation schemas",
    "types/index.ts - Type exports",
    "index.ts - Hono application entry point"
  ]
}
