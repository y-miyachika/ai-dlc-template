{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "case \"$TOOL_INPUT\" in *\".env\"*) echo 'BLOCKED: .envファイルへの直接書き込みは禁止されています。.env.exampleを編集してください。' >&2; exit 1;; esac"
          }
        ]
      }
    ]
  }
}
