# Architecture Designer SubAgent

NFR（非機能要件）を考慮した最適なアーキテクチャを設計するSubAgent

## 概要

NFRを満たす最適なアーキテクチャパターンを選択し、トレードオフを明示し、ADR（Architecture Decision Record）を作成します。

## 使い方

### 前提条件

- ユニット分解が完了している（`/units` 実行済み）
- ドメイン設計が完了している（`/design-domain` 実行済み）
- `docs/intents/` にNFRが定義されている

### 実行

```bash
# AI-DLCテンプレートのスラッシュコマンドから
/design-architecture unit1

# または、Intent番号 + Unit番号
/design-architecture 002-001
```

### 生成されるファイル

```
docs/design-artifacts/
├── architecture/
│   └── {Intent番号}-{Unit番号}-{ユニット名}_architecture.md
└── adr/
    └── ADR-{連番}_{タイトル}.md
```

**例**:
- `docs/design-artifacts/architecture/046_order-management_architecture.md`
- `docs/design-artifacts/adr/ADR-001_クリーンアーキテクチャの採用.md`

## アーキテクチャ設計の流れ

### 1. NFR分析

**NFRカテゴリ**:
- パフォーマンス（Performance）
- スケーラビリティ（Scalability）
- 可用性（Availability）
- セキュリティ（Security）
- 保守性（Maintainability）
- 観測可能性（Observability）
- 信頼性（Reliability）

**優先度設定**:
- High: アーキテクチャ選択の制約条件
- Medium: できるだけ満たす
- Low: 余裕があれば対応

### 2. アーキテクチャパターンの候補列挙

**主要パターン**:

#### レイヤードアーキテクチャ（Layered Architecture）
- **適用場面**: CRUD中心、シンプルなビジネスロジック
- **メリット**: シンプル、実装コスト低
- **デメリット**: 複雑化すると保守困難

#### ヘキサゴナルアーキテクチャ（Ports & Adapters）
- **適用場面**: 外部システムとの連携が多い
- **メリット**: テストしやすい、外部変更に強い
- **デメリット**: 初期実装コスト高

#### クリーンアーキテクチャ（Clean Architecture）
- **適用場面**: ビジネスロジックが複雑、長期保守
- **メリット**: ビジネスロジックが独立、技術的変更に強い
- **デメリット**: 学習コスト高、初期実装コスト高

#### イベント駆動アーキテクチャ（Event-Driven Architecture）
- **適用場面**: ユニット間連携、非同期処理
- **メリット**: 疎結合、スケーラビリティ高
- **デメリット**: 複雑性高、デバッグ困難

#### サーバーレスアーキテクチャ（Serverless）
- **適用場面**: 短時間処理、トラフィック変動
- **メリット**: スケーラビリティ自動、運用コスト低
- **デメリット**: コールドスタート、実行時間制限

### 3. トレードオフ分析

各候補を以下の観点で評価：

**評価項目**:
- **NFR評価**: 各NFRを⭐1-5で評価
- **実装コスト**: Simple / Medium / Large
- **リスク**: 影響度 × 発生確率
- **ドメインモデルとの適合性**: ドメイン設計との整合性

### 4. 推奨オプションの決定

**評価基準**:
1. NFR適合度（最重要）
2. 実装コスト
3. リスク
4. 保守性
5. 拡張性

**トレードオフの明示**:
- 採用オプションのデメリットを明示
- 受け入れ理由を説明

### 5. アーキテクチャ詳細設計

**成果物**:
- コンポーネント構成図（Mermaid）
- レイヤー構成
- データフロー
- ディレクトリ構造
- 技術スタック

### 6. ADR（Architecture Decision Record）作成

**ADRの構成**:
- コンテキスト（なぜこの決定が必要か）
- 決定内容（何を決定したか）
- 考慮した選択肢（3つのオプション）
- 決定理由（なぜこの選択肢か）
- 結果（この決定による影響）
- トレードオフ（受け入れたトレードオフ）

## NFR駆動の設計原則

### ✅ ベストプラクティス

1. **NFRを最優先**
   ```
   NFR: API応答3秒以内
     ↓
   パターン選択: 段階的取得アーキテクチャ
     ↓
   実装: depth指定、部分取得、キャッシュ
   ```

2. **トレードオフの明示**
   ```
   採用: オプションB（段階的取得）
   トレードオフ:
   - 複雑性が増す（許容）
   - 初期実装コスト高（NFR達成のため必要）
   ```

3. **段階的詳細化**
   ```
   1. パターン選択（抽象レベル）
   2. コンポーネント構成（中間レベル）
   3. 技術スタック（具体レベル）
   ```

### ❌ アンチパターン

1. **NFRを無視した設計**
   ```
   パターン選択: 一括取得（シンプル）
     ↓
   NFR未達: 10秒かかる（要件違反）
   ```

2. **トレードオフの隠蔽**
   ```
   採用: オプションA（シンプル）
   理由: 実装が簡単
   （NFRへの言及なし）
   ```

3. **過度な抽象化**
   ```
   将来の要件を推測して複雑化
   → YAGNI（You Aren't Gonna Need It）違反
   ```

## 出力例

### アーキテクチャ設計ドキュメント

**ファイル**: `docs/design-artifacts/architecture/046_order-management_architecture.md`

```markdown
# アーキテクチャ設計: order-management

**元のユニット定義**: `docs/units/046_units.md`
**ドメイン設計**: `docs/design-artifacts/domain/046_order-management_domain.md`

---

## NFR分析

| カテゴリ | 要件 | 目標値 | 優先度 | 理由 |
|---------|------|--------|--------|------|
| パフォーマンス | API応答時間 | 3秒以内 | High | ユーザー体験に直結 |
| スケーラビリティ | 同時接続数 | 100件 | Medium | 初期フェーズのため |
| 可用性 | 稼働率 | 99.9% | High | ビジネス要件 |

---

## アーキテクチャパターン候補

### オプションA: レイヤードアーキテクチャ

**概要**: Presentation, Application, Domain, Infrastructure の4層構造

**メリット**:
- シンプルで理解しやすい
- 実装コストが低い

**デメリット**:
- ビジネスロジックが複雑になると保守が困難

**NFR評価**:
| NFR | 評価 | 理由 |
|-----|------|------|
| パフォーマンス | ⭐⭐⭐☆☆ | 層間の処理で若干のオーバーヘッド |
| スケーラビリティ | ⭐⭐⭐☆☆ | 水平スケーリングは可能 |

（以下、オプションB、オプションCも同様）

---

## 推奨: オプションB - クリーンアーキテクチャ

**推奨理由**:
1. **NFR適合度**: パフォーマンス、可用性の要件を満たす
2. **実装コスト**: Medium、現実的な期間で実装可能
3. **保守性**: 長期的に保守しやすい

（以下略）
```

### ADR

**ファイル**: `docs/design-artifacts/adr/ADR-001_クリーンアーキテクチャの採用.md`

```markdown
# ADR-001: クリーンアーキテクチャの採用

**日付**: 2025-11-19
**ステータス**: Accepted
**決定者**: AI-DLC Framework

---

## コンテキスト

order-managementユニットのアーキテクチャパターンを選択する必要がある。

**NFR（非機能要件）**:
- API応答時間: 3秒以内（High）
- 可用性: 99.9%（High）

---

## 決定内容

**採用するアーキテクチャパターン**: クリーンアーキテクチャ

（以下略）
```

## AI-DLC原則との対応

| 原則 | 実装方法 |
|-----|---------|
| **損失関数** | NFR未達を早期検出（設計段階で） |
| **段階的詳細化** | ドメインモデル → アーキテクチャ → 実装 |
| **設計技術の統合** | DDD原則を保ちつつアーキテクチャ選択 |
| **トレーサビリティ** | NFR → アーキテクチャ選択 → ADR |

## 他プロジェクトでの利用

このSubAgentは、AI-DLCテンプレート以外のプロジェクトでも利用可能です：

1. `.claude/agents/architecture-designer/` をコピー
2. プロジェクトに配置
3. SubAgentを呼び出し

## バージョン履歴

- **1.0.0** (2025-11-19): 初版リリース
  - NFR分析
  - アーキテクチャパターン候補列挙（5パターン対応）
  - トレードオフ分析
  - ADR生成
  - コンポーネント構成図、データフロー作成

## 品質基準

出力品質を確保するため、[品質チェックリスト](../quality-checklist.md)を参照してください。

## ライセンス

MIT
